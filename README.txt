Para instalar as bibliotecas necessárias use o comando:
# pip install -r requirements.txt

Para criar o arquivo executavel use o comando:
# python setup.py build

O executavel do programa estara na pasta build gerada pelo último comando